#include <bits/stdc++.h>
#define ll long long

using namespace std;

ll dp[128][128];

int main() {
    ios_base::sync_with_stdio(0);
    cin.tie(0);

    short n, p, s;
    cin >> n >> p >> s;

    ll res = 0;
    for (short d = 1; d <= p - 1; ++d) {
        for (short i = 0; i <= (s - d)/p; ++i) {
            
        }
    }
}